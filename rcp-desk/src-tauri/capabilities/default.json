{
  "$schema": "https://schema.tauri.app/schema.json",
  "identifier": "default-capability",
  "description": "Default capabilities for RCP Desk",
  "windows": ["main"],
  "permissions": [
    "dialog:allow",
    "shell:allow",
    "app:allow",
    "window:allow"
  ]
}
